export interface SeniorInfoProps {
  image: string;
  name: string;
  position: string;
  description: string;
  socialLink: string;
  bgColor?: "white" | "gray";
  imagePosition?: "left" | "right";
}